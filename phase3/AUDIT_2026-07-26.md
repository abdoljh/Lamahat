# Audit of the third-screening artifacts vs the P7 batch (2026-07-26)

Source material: the user's `_Phase3` Drive folder — `output_files.zip`
(total-solution run, Jul 16), `Archive.zip` (user-revised dossier, Jul 26),
`output_files_ro.zip` (render-only cut `final_cut_B.mp4`, Jul 26).  Text
artifacts extracted to `_Phase3/audit/` and analysed against the P7 code on
branch `claude/phase3-movie-quality-d1n58l`.

## The run being judged

88-shot plan (54 image + 29 typography + 4 section_mark + 1 title_card),
418 s narration, whisper alignment (652 words).  Final cut: Family B,
2.5D parallax ON, typography-over-image ON, **captions OFF**, warm grade,
music −12 dB ducked, 2395 s render, 197.5 MB.

## Note 1 — "not all shots are listed"  → CONFIRMED, fixed by P7.1

Dossier contains 54 folders for exactly the 54 image shots; the 34
typography-kind shots (39 % of the film) have no folder at all — shot_01,
shot_03, shot_06 … read as "cut from the script".  P7.1 gives every shot a
folder + card preview.  Also found: 4 starred folders (29, 43, 72, 78 —
waterfall starvation) and **38 of 54 image shots hand-replaced** with
`user_`/`my_` files — the curated set has effectively become the film's
real photo source.

## Note 2 — "sentence cut across shots"  → CONFIRMED, worse than reported

Boundary classification of the 87 interior cuts against the narrated
punctuation stream:

| kind | count |
|---|---|
| on a sentence end | 11 |
| on a clause end (، ؛) | 3 |
| **mid-sentence** | **73 (84 %)** |

The P7.2 post-hoc snapper (±0.9 s) rescues only 12 of the 73 — this plan's
cuts are too far from sentence ends to nudge.  **The prompt fix is the real
fix; a re-PLAN is required, not a re-render.**  The sentence caption track
built from this narration: 80 events, avg 5.2 s, 60 of which span at least
one cut (i.e. 60 subtitles would previously have been chopped).

## Note 3 — era restriction  → CONFIRMED, quantified

44 of 54 image shots (81 %) carry a period era; only 10 are timeless.  The
44 period shots use **37 distinct era strings** ("1900s Ottoman Empire
multi-ethnic officer corps", "1910s Ottoman Empire political decay", …) —
each one a separate archival-search demand, guaranteeing starvation under
the P6 hard gate.  P7.3 softens the gate (era-0-only rejection, Pexels as
era-gated last resort) and steers thematic beats to "timeless".

## NEW finding — script/narration divergence (word 407)

The narration (and word_timings.json) says
«أم ينضم إلى صفوف الثورة العربية ضد الحكم العثماني؟» where every stored
`main_script.txt` says «أم ينحاز إلى شعبه العربي الذي يحلم بالحرية؟».
One revised sentence → every later word is offset by one token (652 vs
651).  The rendered cut is safe (plan quotes match the audio), but any
future align/plan run mixing the revised script with the old narration will
misalign the last third of the film.  **Rule: after any script edit, either
re-synthesize the narration or revert the script — never mix.**
P7.2's length-mismatch guard degrades gracefully here (falls back to
token-tail punctuation), by design.

## Recommendations for the P7 run

1. Fix the script/narration pair first (cheapest: edit `main_script.txt`
   to the narrated sentence).
2. Run the **total solution** on `claude/phase3-movie-quality-d1n58l` —
   the planner prompt changed, so a re-render of the old plan cannot fix
   note 2 (84 % mid-sentence cuts are baked into that plan).
3. Move the 38 curated `user_`/`my_` images into `resources/photo_bank/`
   — the Sonnet bank assignment matches by CONTENT, not shot number, so
   the curation survives replanning (shot numbers will change).
4. Try captions ON: the sentence-level track (v2 plan `captions`) is new
   since this cut was rendered with captions off.

## Follow-up (2026-07-27) — fourth-screening run audited

The P7 total solution ran (82 shots, 80 sentence captions, whisperx,
vision on) but the user judged it no better.  Three causes found:

1. **Photo-bank assignments 100 % discarded by a parsing bug.**  Sonnet
   assigned 27 bank photos to shots but returned keys as `"shot 2"`;
   `int("shot 2")` raised and every assignment was dropped with only a
   per-key warning.  This bug ate the bank in BOTH screenings.  Fixed:
   the validator now extracts the integer from any key shape, prebuild
   prints a loud banner + writes `PHOTO_BANK_NOT_ASSIGNED.txt` when a
   populated bank yields zero assignments.
2. **Cuts improved 84 % → 67 % mid-sentence — not enough.**  The ±0.9 s
   snap window was too timid.  Now a hard grid: ±2.6 s to a sentence
   end, ±1.2 s to a clause end, monotonic walk.  Replayed on the real
   82-shot plan: 54 → 33 mid-sentence cuts; a fresh plan compounds the
   prompt and the grid.
3. **Portraits collapsed 9 → 3** — the P7.3 "prefer timeless" guidance
   over-corrected.  The prompt now carries a biography portrait floor
   (6–10 protagonist portraits; timeless metaphors must not displace
   the people of the story).

Also confirmed: the narration is authoritative (it never says "ينحاز");
whisperx aligned 651/651 tokens, so the script mismatch is not a
blocker for rendering.  Captions were OFF in both screenings — the
sentence-caption track has not yet been seen on screen.

## Fifth screening (2026-07-28) — total solution, captions ON: real progress

User's read, independently confirmed against the run's artifacts before
comparing notes: **this run is better than all previous ones.**

- Photo bank: 25/51 image shots (49%) + 9/51 (18%) from the character
  pool = **67% curated imagery**, up from 0% (the `"shot 2"` key-parsing
  bug is fixed and held).
- Portraits: 11 shots (was 3, was 9 originally) — biography floor working.
- Mid-sentence cuts: 84% → 67% → **54%** — real but incomplete progress;
  the hard grid helps, still a majority-ish issue.
- Era mix: 30 period / 21 timeless, healthy.
- Notebook `BRANCH="main"` is fine — P7.6 is merged to `main` already
  (confirmed by reading `photo_bank.py` directly off that branch).

### Three new bugs found — from watching the actual output, not logs (P7.7)

Screenshots caught what audit-log analysis structurally cannot: what the
audience actually sees. All three root-caused against this run's real
`shot_plan.json` (84 shots, 80 caption events) and reproduced pixel-for-
pixel with a direct `ffmpeg`+`libass`+Amiri render — not inferred.

1. **Title card text leaking into the next shot's caption.** Event 0
   spanned the title card (hidden, 0.13–4.13s) AND the opening narration
   sentence; `_write_captions` correctly computed the VISIBLE time span
   after clipping but then burned the event's FULL text regardless —
   so shot 2's caption showed "مذكرات جعفر العسكري" (the title, spoken
   during the hidden portion) glued onto the real caption. Root cause:
   `CaptionEvent` only carried aggregate start/end/text, no word-level
   timing, so a clipped sub-span had no way to reconstruct partial text.
   Fixed: `CaptionEvent.words` (word, start, end) triples; a clipped
   span now burns only the words whose timing falls inside it.

2. **Whole clauses silently vanishing.** "وترك لنا سجلا دقيقا عن حقبة لم
   يعرفها سوى من عاشوها بكل تفاصيلها" never appeared at all. Cause: the
   sentence's tail fell entirely within a `broll` shot the planner had
   marked `show_caption=False`, immediately followed by a `typography`
   shot (inherently hidden) — the two hidden ranges were contiguous, so
   the ENTIRE remaining span of that sentence clipped to nothing.
   `show_caption=False`'s old semantics (from the per-shot caption
   system) meant "this shot's own self-contained caption is redundant" —
   in the sentence-track system that same flag on a real-image shot now
   silently deletes narration. Fixed: events-mode hidden ranges come
   ONLY from `TYPOGRAPHY_VISUALS` shots; `show_caption` on image-kind
   shots no longer affects the sentence track.

3. **Trailing Arabic comma renders at the wrong end of the line.** A
   caption ending "...وطنه،" burned as "،...وطنه" — the comma visually
   escaping to the START of the RTL line instead of trailing it.
   Reproduced directly (isolated from this codebase) with `ffmpeg`+
   `libass`+Amiri: any caption text or wrapped line ending in ، ؛ or ؟
   with nothing after it exhibits this — a known libass/fribidi neutral-
   character bidi resolution gap when a trailing mark has no following
   strong character to anchor it. Confirmed NOT digit-related (`سنة
   ١٩٠١،` and bare `وطنه،` show the identical bug) and confirmed
   per-visual-line (the first line of a `\N`-wrapped two-line caption
   needs its own fix, independent of the whole Dialogue field's true
   end). Fix verified empirically across 5 candidate approaches — only
   an invisible RLM (U+200F) appended immediately after the trailing
   mark anchors it correctly; `_wrap_caption` now applies this to each
   visual line independently via `_anchor_trailing_punct`.

All three verified end-to-end: real `shot_plan_v3.json` word timings run
through the fixed `_write_captions`, then actually burned to pixels via
`ffmpeg`+`libass` — not just ASS-text inspection. This bug class exists
BECAUSE the sentence-track shipped in P7.2 (a per-shot caption system
never had cross-shot text or cross-shot hidden-range clipping to get
wrong) — screening real output past the log-analysis stage is what
caught it.

## Render-only re-check (2026-07-28, same dossier) — bug 1 still visible, root cause found

User re-screened via the render-only route (same dossier reused, no
re-plan) and sent 3 screenshots + a 50 s clip. Result: bug 3 (comma)
confirmed fixed; bug 1 (title/first-sentence leak) still reproduces,
plus a new-looking leak with "الحركة القومية العربية،"; bug 2 needed a
fresh check.

**Root cause, confirmed against the actual dossier, not guessed:**
`shot_plan_v3.json` — the exact plan this dossier's render-only pass
reuses — has 80 caption events with **`words: []` on every one of
them** (checked directly: `n_old_with_words == 0`). That plan was
written by the total-solution run that produced this dossier, which
ran *before* commit `873f9d8` (P7.7, 2026-07-28 09:11:50 UTC) landed —
`CaptionEvent.words` didn't exist yet when it was saved. `_write_captions`'s
fix falls back to the event's full `text` whenever `words` is empty, so
render-only — which is offline and reuses the dossier's `shot_plan.json`
verbatim — mechanically reproduces the pre-fix symptom regardless of
which commit the Streamlit app itself is running. This is a **stale
dossier**, not a code regression.

Confirmed pixel-for-pixel against the user's clip: extracted frame at
t=33s (inside caption event 5's clipped visible span, 32.171–36.696,
right after hidden typography shot 6 at 26.945–32.171) shows the full
unclipped event text "الحركة القومية العربية، وترك لنا سجلا دقيقا عن
حقبة لم يعرفها سوى" — including "الحركة القومية العربية،", which was
spoken *during* the hidden card, not during the visible span. Exactly
the mechanism predicted.

Bug 2 re-checked in the same clip: at t=38s (event 6, "من عاشوها بكل
تفاصيلها", no adjacent hidden range) the sentence renders in full —
the show_caption fix **is** working; the words leaking in is a separate,
already-diagnosed issue (bug 1), not a recurrence of bug 2.

**Fix**: `regenerate_captions.py` (new) — rebuilds only the `"captions"`
array of an existing dossier's `shot_plan.json` from the dossier's
already-saved `word_timings.json` + `script.txt` via `build_caption_events()`.
Pure local recomputation: no Claude API call, no re-fetch, shots/images
untouched. Tested against `shot_plan_v3.json` + `word_timings_v3.json`:
regenerates the same 80 events, now all carrying `words`, and the
event-5 word list confirms "العربية،" (ends 32.17s) falls outside the
32.121–36.746s visible-span window while "وترك" (starts 32.27s) falls
inside it — the leak is gone at the data level. Run once against the
existing dossier, then re-render-only at zero extra API cost:

```bash
python regenerate_captions.py --review-dir output/review/
```

## P7.8 (2026-07-29) — why the repair changed nothing: two shot_plan.json copies

User ran `regenerate_captions.py` in the render-only notebook, it
reported success (`wrote output/review/shot_plan.json`), and the next
render was **identical** — same title leak, same "الحركة القومية
العربية" repetition. They also noticed `shot_plan.json` and
`word_timings.json` "appear in more than one location".

That observation *is* the bug. The render-only notebook sets
`PLAN_FILE = "output/shot_plan.json"` and `REVIEW_DIR = "output/review/"`,
and the render log's own header confirms which one it read:

```
Plan   : output/shot_plan.json  (84 shots, 80 sentence captions)
Review : output/review/ (dossier consulted before fetcher)
```

The script patched `output/review/shot_plan.json`; the renderer read
`output/shot_plan.json`. Two files, one patched, the wrong one used —
so a perfectly working fix produced a byte-identical film.

**Durable fix — the renderer now self-heals, so the copy question stops
mattering.** `plan.repair_caption_events()` runs at load time in both
render entry points (`render_plan.py`, `render_from_review`): when a
plan's caption events carry no `words`, it finds a `word_timings.json`
beside the plan or in the dossier and attaches per-word timing to each
event. Conservative — an event is only repaired when the words inside
its own time range join back to EXACTLY its stored text, so a mismatched
narration leaves it untouched rather than burning something never said.
Verified on the real stale plan: 80/80 events repaired, all passing the
text-identity check.

`regenerate_captions.py` now also repairs **every** `shot_plan.json` it
finds (review dir *and* its parent) and prints each path, so running it
by hand can't miss a copy either.

### Second finding — the correct fix produced one- and two-word stubs

With word-level clipping working, 22 of 61 caption lines changed, and
7 of them collapsed to fragments like "أن" (0.94 s), "كان" (1.46 s),
"لكن جعفر", "بين قديم" — the tail of a sentence whose body was spoken
under a typography card. Technically correct (that *is* all that was
audible during the visible span) but it reads as flicker, not subtitle.
Since the typography card carries that sentence's own text on screen,
the viewer has already read it. `_write_captions` now drops a **clipped**
span shorter than `MIN_CLIPPED_WORDS = 3` words; unclipped short events
are untouched. Net: 61 → 54 caption lines on this plan.

Both reported symptoms verified gone by burning the resulting ASS to
real pixels with `ffmpeg`+`libass`+Amiri at the two timestamps the user
photographed:

| t | before | after |
|---|---|---|
| 5.5 s (shot 2, portrait) | `مذكرات جعفر العسكري رجل واحد` \ `حمل سلاحا من أجل تحرير وطنه،` | `سلاحا من أجل تحرير وطنه،` |
| 33.5 s (shot 8, broll) | `الحركة القومية العربية، وترك لنا سجلا` \ `دقيقا عن حقبة لم يعرفها سوى` | `وترك لنا سجلا دقيقا` \ `عن حقبة لم يعرفها سوى` |

No re-plan, no API cost: re-run render-only on the same dossier.

## P7.9 (2026-07-29) — the real root cause: cards are not on their own words

User, after the P7.8 render: *"The latest version only fixed part of the
current issues! It seems clearly that the source of these issues is
inaccurate synchronization."* Their evidence, straight from the plan:

```
shot 1  typography_text: "مذكرات جعفر العسكري"
        caption_text   : "مذكرات جعفر العسكري رجل واحد حمل"
shot 3  typography_text: "أشرس الأعداء قد يكونون من داخل صفوفك."
        caption_text   : "لكنه اكتشف أن أشرس الأعداء قد يكونون من داخل صفوفك"
```

They are right, and it is the root cause — everything before this was
symptom management. Measured across that plan's 33 typography cards, by
locating each card's text in the narration word timings:

| | |
|---|---|
| cards within 0.5 s of the words they quote | **4 / 33** |
| median drift (card in/out vs its own words) | **3.10 s** |
| worst | card up **10.0 s before** its words, gone 12.3 s before they end |

A card also suppresses the subtitle track. So a drifted card shows
sentence A in silence while the narrator speaks sentence B: the viewer
reads one thing, hears another, and gets no subtitle for either. Both
of the user's reported symptoms fall out of this directly — shot 1's
card covers only the first 3 words of a 6-word span, so "رجل واحد حمل"
went uncaptioned and the caption resumed mid-phrase.

**`plan.sync_typography_to_speech()`** repairs each card, choosing per
card between two bounded edits:

* **trim** (8 cards) — the card's words are inside its own shot, so the
  shot tightens onto them and the freed time goes to the *immediate*
  neighbour, if that neighbour has room under `HARD_CAPS`.
* **retext** (20 cards) — the card quotes something spoken elsewhere, so
  the card is rewritten to the words actually spoken under it. It loses
  the planner's editorial trim and gains the only thing that matters: it
  says what the audience is hearing.

A first attempt reflowed the whole timeline to place every card exactly.
It scored better on paper (27/31 cards within 0.5 s) but pushed **13
image shots past their duration caps** — reintroducing the static-hold
problem this project spent P7.4 fixing. Rejected in favour of the local
version, which touches only neighbours.

| | before | after |
|---|---|---|
| card says what is heard (token IoU, median) | 0.50 | **1.00** |
| cards at ≥0.8 agreement | 5/33 | **30/33** |
| shots / over-cap shots | 84 / 0 | 84 / **0** |
| longest shot | 7.87 s | 7.87 s |

Shot count, order and contiguity are preserved, so a dossier keyed by
shot index stays valid — this runs on an existing dossier with no
re-plan and no API cost. Wired into `build_shot_plan` (after
`_validate_plan`, before `_fill_captions`) and into
`regenerate_captions.py` (`--no-sync-cards` opts out).

The opening, burned to pixels, before and after the whole P7.7–P7.9 arc:

| | caption over shot 2 (the portrait) |
|---|---|
| P7.6 and earlier | `مذكرات جعفر العسكري رجل واحد` \ `حمل سلاحا من أجل تحرير وطنه،` — title duplicated |
| P7.8 | `سلاحا من أجل تحرير وطنه،` — leak gone, but starts mid-phrase |
| **P7.9** | `رجل واحد حمل سلاحا من أجل تحرير وطنه،` — the whole clause, on the man, as it is spoken |

Residual, deliberately not fixed: 3 of 33 cards still hold slightly past
their line because both neighbours are already at their duration cap, so
there is nowhere to give the time back. Those cards say the right words;
they just linger. Fixing it needs a re-plan, not a repair pass.

## P7.9 correction (2026-07-29) — retext screened as a regression, reverted

Self-audit against the first-minute render-only cut had already flagged
retext as risky before the user watched it: 10 of 20 rewritten cards
ended on a dangling preposition or bare comma. The user's 2:00–5:30
sample confirmed it was worse than that assessment — and caught a
second bug the first minute never exercised (no split card falls in the
opening).

**Screened, not just read.** Two concrete failures, both timestamped
against the actual video:

1. **The typographic punch was gone.** Shot 34's card, text unchanged:
   `انقلابات ومكائد تحاك في كل زاوية.` — bold, full-weight, lands as a
   statement. Shot 42's card, retexted: `ومصالح شخصية تعترض طريق
   الإصلاح الحقيقي كانت هذه فترة من` — the same template, same family,
   but the engine's auto-fit shrinks the longer line to a thin, small,
   grammatically severed fragment ending on "من" ("of"). Same at 2:17
   and 4:12. Sync had been optimised as a text-agreement metric; the
   metric couldn't see that it was quietly destroying the pull quote as
   a design element at exactly the beats meant to carry the most
   weight.
2. **Split-card headings broke on screen, exactly as predicted from the
   plan before rendering.** Shots 30/31 (`الصراع الأيديولوجي والسياسي —
   بين الولاء والحلم`, split for motion variety) were retexted
   *independently per piece* — confirmed at 2:26 showing `الصراع` alone
   over a desert dune, then 2:28 showing `والسياسي بين`. Same for
   shots 62/63 (`الخاتمة: شهادة لا تموت` → `الخاتمة` at 5:05, `لا تموت`
   at 5:08). One heading, two meaningless fragments.

**Fix — retext removed entirely; `typography_text` is never rewritten.**
A new `Shot.card_hides_captions` field (default `True`, matching every
existing plan) replaces it: when a card's words are NOT what's playing
during its span, `sync_typography_to_speech()` sets the flag `False`
instead of touching the text, and `render._write_captions`'s
hidden-range computation now honours it — the card stays on screen
exactly as the planner wrote it, and the sentence-caption track keeps
running underneath instead of going dark. Verified with a real
`ffmpeg`+`libass` composite that a lower-third pull quote (anchor 0.63)
and the bottom-edge caption bar (margin 0.06) don't visually collide.
Re-run on the reference plan: split headings 30/31 and 62/63 both keep
their full original text; 0 cards have their text changed (was 20); 33
of 33 cards now have their voice audible (13 by trim, 20 by uncovering
captions), up from the 5/33 that were already in sync before any of
this work started.

**A second, unrelated bug found in the same screening — not a caption
bug at all.** The opening portrait (shot 2, noted "first glimpse of
protagonist") showed a different image on every render-only pass of the
*identical* dossier: `jafar_in_uniform.jpg`, then `jafar_in_office.jpg`,
then — the render the user actually watched — `memoirs_3dw2.jpg`, a 3-D
book-cover mockup, not a portrait. Root cause: `decisions.
_list_portrait_pool` seeds its shuffle with `hash(tuple(names))`, and
Python salts `str`/`tuple` hashing per interpreter process
(`PYTHONHASHSEED`) — confirmed directly, three processes gave three
different seeds despite the docstring's claim of reproducibility. Fixed
with an unsalted `hashlib.sha256` seed (confirmed identical across
processes). The `memoirs_*` book-mockup images being IN the character
pool at all is a separate curation question, left to the user — the
opening now opens on the same face every render-only pass, whichever
face that pool draws.

## P7.9 second correction (2026-07-30) — uncovering was too eager, caused duplicate text

User re-screened a fresh 0:00–5:00 render-only cut against the three
fixes above and reported "2 and 3 are fine but not 1" (the
`card_hides_captions` fix). Confirmed on screen, not guessed: at 0:55
(shot 12, section_mark `من الموصل إلى الاستانة — رحلة التحديث
والطموح`) the caption underneath read `من الموصل إلى الاستانة رحلة
التحديث والطموح` — the same sentence, once large as the card, once
small as the caption, at the same time. Same at 0:28 (shot 7).

Root cause: `_locate_card`'s match for these two cards **does** fall
mostly inside their own shot — they missed the strict trim window
(which requires BOTH ends within `tolerance=0.35s`) only because the
sentence runs a couple of seconds into a neighbouring shot. Measured
coverage (fraction of the located quote's words actually spoken inside
the card's own on-screen span): shot 7 = 0.64, shot 12 = 0.57. My
Case 2 treated "missed the trim window" as equivalent to "displaced
elsewhere" and uncovered both — putting a near-duplicate of the card's
own words on screen as the caption. Shot 16, the case that DID look
right to the user, has coverage 0.00 — its caption shows a genuinely
different, complementary sentence, which is why side-by-side reads
fine there.

Fix: `sync_typography_to_speech` now computes this coverage and only
uncovers when it is **< 0.5** — a card whose quote is mostly inside its
own span stays hidden even though it missed the trim window, since a
slightly early/late caption is a smaller sin than showing the same
sentence twice at once. Re-run on the reference plan: uncovered count
dropped 20 → **9**; both duplicate instances (shots 7, 12) confirmed
gone from the ASS output (no `Dialogue` line falls inside either
shot's span); shot 16's complementary caption is unchanged.

Also fixed in the same pass: split-card runs (identical text, two
shots for motion variety) are now judged ONCE over their **combined**
span and given the same decision — shots 30/31 and 62/63 had
individually different coverage (0.33 vs 0.50) that would otherwise
have hidden captions under one half of a heading and shown them under
the other. Both now consistently hidden.
